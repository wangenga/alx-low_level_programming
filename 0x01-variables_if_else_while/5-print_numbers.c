#include <stdio.h>
#include <stdlib.h>
/**
 * main-exectable function
 * Description:Prints all single digits of base 10 from 0
 * Return:Always 0(success)
 */
int main(void)
{
	int x;

	for (x = 0; x < 10; x++)
		printf("%d", x);
	return (0);
}
