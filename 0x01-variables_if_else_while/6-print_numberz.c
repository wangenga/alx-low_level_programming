#include <stdio.h>
/**
 * main-executable function
 * Description-prints single digit numbers of base 10
 * Return:Always 0(success)
 */
int main(void)
{
	int x;

	for (x = 0; x < 10; x++)
		printf("%d\n", x);
	return (0);
}
