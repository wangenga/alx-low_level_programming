#include <stdio.h>
/**
 * main-executable function
 * Description:print the lowercase alphabet
 * Return:Always 0(success)
 */
int main(void)
{
	int x;

	for (x = 'a'; x <= 'z'; x++)
	{
		putchar(x);
	}
	putchar('\n')
	return (0);
}
