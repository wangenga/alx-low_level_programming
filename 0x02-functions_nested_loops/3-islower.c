#include "main.h"
#include <stdio.h>
/**
 * main-executable function
 * Description:checks for lowercase letters
 * Return:Always 0(success)
 */
int _islower(int c);
{
	if( c = 'a' && c < 'z')
	{
	return (1);
	}
	else
	{
	return (0);
	}
}	
