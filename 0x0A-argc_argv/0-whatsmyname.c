#include <stdio.h>
/**
 * main - prints name
 * @argc: the number of arguments
 * @argv: array of pointer
 * Return: Always 0
 */
int main(int argc, char *argv[])
{
	argc = argc;
	printf("%s\n", argv[0]);
	return (0);
}
